{
  "name": "ts-action",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "pack": "bun build --target=node --outdir=dist index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  }
}
